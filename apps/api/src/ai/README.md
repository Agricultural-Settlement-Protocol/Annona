# AI assistant

Plain-Bahasa Q&A for the auditor/petinggi over indexer read-models.
Read-only, grounded: every figure links to a source agreement/event, no
hallucinated numbers. Gemini Flash (swappable for Claude Haiku via a provider iface).

Output rule: NO em dashes in any user-facing answer (CLAUDE.md NOTES).

MVP status: cuttable (first to cut, PRD 7.11). Build only if time remains.
