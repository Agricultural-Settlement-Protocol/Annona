{
  "name": "@annona/scripts",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Demo seed + deploy drivers. Not shipped; run in the workspace.",
  "scripts": {
    "seed": "tsx seed.ts",
    "check-types": "tsc -p tsconfig.json",
    "seed:chain": "tsx seed-chain.ts"
  },
  "dependencies": {
    "@annona/api": "workspace:*",
    "@annona/core": "workspace:*",
    "@annona/ui": "workspace:*",
    "@stellar/stellar-sdk": "^16.0.1",
    "dotenv": "^17.2.3",
    "drizzle-orm": "^0.45.2"
  },
  "devDependencies": {
    "@annona/config": "workspace:*",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
