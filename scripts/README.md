# scripts/

Operational scripts for Annona. To be implemented alongside the contract.

| Script | Purpose |
|---|---|
| `fund-testnet.sh` | Friendbot-fund coop + farmer demo accounts on testnet. |
| `deploy.sh` | `stellar contract build` + `deploy` offtake-registry + didr-token, run `init`. |
| `seed.ts` | Create ~10 realistic farmers + agreements (gabah/jagung) for the demo. |

Status: placeholders. Build after `contracts/` is scaffolded.
